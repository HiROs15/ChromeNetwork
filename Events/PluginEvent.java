package dev.chromenetwork.prison.Events;

import org.bukkit.event.Listener;

public class PluginEvent implements Listener {
	// Lol this is just so I can load events and register them in EventManager
}
